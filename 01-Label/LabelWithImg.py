from Tkinter import *

root = Tk()

label = Label(root,bitmap = 'hourglass')
#bitmap :  error * hourglass * info * questhead * question * warning * gray12 * gray25 * gray50 * gray75

label.pack()

root.mainloop()
